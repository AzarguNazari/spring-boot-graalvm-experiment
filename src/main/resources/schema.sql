-- drop table if exists Tutorial;


CREATE TABLE Tutorial (id INTEGER NOT NULL AUTO_INCREMENT,
                       title VARCHAR(255),
                   description varchar(500),
                       published varchar(5),
                   PRIMARY KEY (id)
);
